using System.Threading.Tasks;
using NUnit.Framework;

namespace ioList.Tests
{
    [TestFixture]
    public class ProjectConnectionTests
    {
        [Test]
        public async Task Connect_LetsSee()
        {
            /*var connection = new ProjectConnection();

            var db = await connection.Connect(new ProjectFile(), CancellationToken.None);

            db.Should().NotBeNull();

            FileAssert.Exists(Path.Combine(Environment.GetFolderPath(Environment.SpecialFolder.MyDocuments),
                @"ioList\Projects\MyProject.db"));*/
        }
    }
}