<Window x:Class="ioList.Shell"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:ioList="clr-namespace:ioList"
        xmlns:shared="clr-namespace:ioList.Shared"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="{Binding Title}"
        ResizeMode="{Binding ResizeMode}"
        WindowStartupLocation="CenterOwner"
        Style="{DynamicResource ShellStyle}"
        d:DataContext="{d:DesignInstance ioList:ShellViewModel, IsDesignTimeCreatable=True}"
        mc:Ignorable="d"
        d:DesignHeight="600" d:DesignWidth="800">

  <Border BorderBrush="{StaticResource Brush.Light.Background}">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition />
        <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>
      
      <ContentControl Grid.Row="0" prism:RegionManager.RegionName="{x:Static shared:Regions.ContentRegion}" />
      <ContentControl Grid.Row="1" prism:RegionManager.RegionName="{x:Static shared:Regions.FooterRegion}" />
    </Grid>
  </Border>
</Window>